version: "3.9"

services:
  dist-imaging:
    build:
      context: .
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: bash -c "scripts/bootstrap.sh && scripts/build.sh && scripts/run_all.sh"

